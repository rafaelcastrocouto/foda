game.heroesAI.cat = {
  move: {
    default: 'offensive'
  },
  play: function (card, cardData) {

    //card.data('ai', cardData);
  },
  defend: function (card, cardData) {

    //card.data('ai', cardData);
  }
};